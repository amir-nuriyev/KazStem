# Corresponding source for KazStem {{VERSION}} ({{TARGET}})

This archive is the source counterpart of `{{READY_FILENAME}}` at
{{READY_URL}}. It contains the exact KazStem application source,
canonical wheel and sdist, resource/build inputs, native Project.JJ source
archives, CPython/PyInstaller source inputs, license material, and identity-bound
release evidence named by `SOURCE-CLOSURE.json`.

Public release: {{RELEASE_URL}}

Nothing in this archive is required at runtime. In particular, native source
archives and OpenSSL source are never embedded in the ready-run binary archive.
